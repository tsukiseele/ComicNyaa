import 'package:cached_network_image/cached_network_image.dart';
import 'package:flutter_cache_manager/flutter_cache_manager.dart';

extension CacheManagerExtensions on DefaultCacheManager {
  static initialize() {
    // BaseCacheManager()..custom(Config());
    // DefaultCacheManager().store.;
    // return _instance!;
  }
}

class CustomCacheManager {
  static initialize() {
    
    // BaseCacheManager()..custom(Config());
    // DefaultCacheManager().store.;
    // return _instance!;
  }
}
